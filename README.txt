A program to send repeated SMS messages at defined intervals.

The "msg.txt" file will contain one message per line -- text messages will send one after the other, then repeat the message list the number of times specified.

Edit recipient phone number and sender email in "config.py" file.